=> 3 External CSS files are required for each of:
  "contact.html", "products.html", "shop.html"
  CSS for index.html is already provided.

=> Link the then created CSS files to their allocated HTML files using
   "<link rel> attribute.

=> Modify the index_css to add suitable color schemes.

=> Create a JS file called "Calculate.js" and add a Javascript function for the
   "calculate" button in shop.html

   Best of Luck
   
   -Tamir